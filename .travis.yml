language: python
python:
    - "3.5"
    - "3.6"
    - "3.7"
install: 
    - "pip install -r requirements.txt"
    - "pip install coverage"
    - "pip install coveralls"
script: 
    - "coverage run --source=pyps4_homeassistant setup.py test"
after_success:
    coveralls
